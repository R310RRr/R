local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Nov23Y/Nov23Y/refs/heads/main/windui"))()
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local whitelist = {
    ["SadNii4"] = true,
    ["EternaIKing_ofc"] = true,
    ["EternalQueen_ofc"] = true,
    ["OfEternal_king"] = true,
    ["OfEternal_queen"] = true,
    ["PROPLAYER292827"] = true,
    ["NOOB_SEUPESADELO"] = true,
    ["SadNii0"] = true,
    ["NeezXIX"] = true,
    ["KillMachineFatality"] = true,
    ["LuaWorldV99"] = true,
    ["WMF0REVER"] = true,
    ["User1"] = true,
    ["User2"] = true,
    ["User3"] = true
}

if not whitelist[LocalPlayer.Name] then
    LocalPlayer:Kick("Not Whitelisted")
    return
end

print("Authorized user:", LocalPlayer.Name)

game:GetService("Players").LocalPlayer.Idled:Connect(function()
    game:GetService("VirtualUser"):Button2Down(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
    wait(1)
    game:GetService("VirtualUser"):Button2Up(Vector2.new(0, 0), workspace.CurrentCamera.CFrame)
end)

local function removePortals()
    for _, portal in pairs(game:GetDescendants()) do
        if portal.Name == "RobloxForwardPortals" then
            portal:Destroy()
        end
    end
    if _G.AdRemovalConnection then
        _G.AdRemovalConnection:Disconnect()
    end
    
    _G.AdRemovalConnection = game.DescendantAdded:Connect(function(descendant)
        if descendant.Name == "RobloxForwardPortals" then
            descendant:Destroy()
        end
    end)
end
removePortals()

WindUI.TransparencyValue = 0.2
WindUI:SetTheme("Dark")

local Window = WindUI:CreateWindow({
    Title = "R310RRr | PRIVATE KILLER",
    Author = "Private Script",
    Size = UDim2.fromOffset(490, 965),
    Theme = "Dark",
    SideBarWidth = 220,
    ScrollBarEnabled = true
})

Window:Tag({
    Title = "Private Killer",
    Color = Color3.fromRGB(0, 0, 0)
})

local Stats = Window:Tab({
    Title = "View Stats",
    Locked = false
})

local SelectPlayerName = ""
local PlayerDropRef = nil
local playerDropdownItems = {}

local function getPlayerDropdownText(player)
    local cleanDisplayName = player.DisplayName:gsub("%s+", " "):gsub("^%s+", ""):gsub("%s+$", "")
    if cleanDisplayName == "" or cleanDisplayName == player.Name then
        return player.Name
    else
        return cleanDisplayName .. " | " .. player.Name
    end
end

local function rebuildPlayerDropdown()
    local oldValue = SelectPlayerName
    local playerNames = {}
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            table.insert(playerNames, getPlayerDropdownText(player))
        end
    end
    
    if PlayerDropRef then
        pcall(function()
            PlayerDropRef:Destroy()
        end)
    end
    
    local dropdownOptions = {
        Title = "Select Player",
        Multi = false,
        Value = "",
        AllowNone = true,
        Values = playerNames,
        Callback = function(Value)
            SelectPlayerName = Value:match("| (.+)") or Value
        end
    }
    
    PlayerDropRef = Stats:Dropdown(dropdownOptions)
    
    if oldValue ~= "" then
        for _, playerName in ipairs(playerNames) do
            if playerName:find(oldValue, 1, true) then
                pcall(function()
                    PlayerDropRef:SetValue(playerName)
                end)
                break
            end
        end
    end
end

rebuildPlayerDropdown()

Players.PlayerAdded:Connect(function(player)
    if player ~= LocalPlayer then
        task.wait(0.5)
        rebuildPlayerDropdown()
    end
end)

Players.PlayerRemoving:Connect(function(player)
    if SelectPlayerName == player.Name then
        SelectPlayerName = ""
    end
    task.wait(0.5)
    rebuildPlayerDropdown()
end)

local function FormatNumberWithCommas(number)
    local formatted = tostring(number):reverse():gsub("(%d%d%d)", "%1,"):reverse()
    return formatted:gsub("^,", "")
end

local function FormatAbbreviated(number)
    local abbreviations = {"", "K", "M", "B", "T", "Qa", "Qi"}
    local abbreviationIndex = 1
    while number >= 1000 and abbreviationIndex < #abbreviations do
        number = number / 1000
        abbreviationIndex = abbreviationIndex + 1
    end
    if number % 1 == 0 then
        return string.format("%.0f", number) .. abbreviations[abbreviationIndex]
    else
        return string.format("%.2f", number):gsub("%.?0+$", "") .. abbreviations[abbreviationIndex]
    end
end

local function FormatDisplay(value)
    local normal = ""
    if value % 1 == 0 then
        normal = FormatNumberWithCommas(math.floor(value))
    else
        normal = string.format("%.2f", value):gsub("%.?0+$", "")
    end
    local abbreviated = FormatAbbreviated(value)
    return " " .. normal .. " [ " .. abbreviated .. " ]"
end

local Update = Stats:Label("Strength:")
local Update1 = Stats:Label("Gems:")
local Update2 = Stats:Label("Rebirth:")
local Update3 = Stats:Label("Agility:")
local Update4 = Stats:Label("Durability:")
local Update5 = Stats:Label("Kills:")
local Update6 = Stats:Label("Muscle King Time:")
local Update9 = Stats:Label("Current Map:")
local Update10 = Stats:Label("Custom Size:")
local Update11 = Stats:Label("Custom Speed:")
local Update12 = Stats:Label("Evil Karma:")
local Update13 = Stats:Label("Good Karma:")

task.spawn(function()
    while task.wait(0.1) do
        if SelectPlayerName ~= "" then
            local player = game.Players:FindFirstChild(SelectPlayerName)
            if player then
                if player:FindFirstChild("Gems") then
                    Update1:Set("Gems: " .. FormatDisplay(player.Gems.Value))
                else
                    Update1:Set("Gems: N/A")
                end
                if player:FindFirstChild("Agility") then
                    Update3:Set("Agility: " .. FormatDisplay(player.Agility.Value))
                else
                    Update3:Set("Agility: N/A")
                end
                if player:FindFirstChild("Durability") then
                    Update4:Set("Durability: " .. FormatDisplay(player.Durability.Value))
                else
                    Update4:Set("Durability: N/A")
                end
                if player:FindFirstChild("muscleKingTime") then
                    Update6:Set("Muscle King Time: " .. FormatDisplay(player.muscleKingTime.Value))
                else
                    Update6:Set("Muscle King Time: N/A")
                end
                if player:FindFirstChild("customSize") then
                    Update10:Set("Custom Size: " .. FormatDisplay(player.customSize.Value))
                else
                    Update10:Set("Custom Size: N/A")
                end
                if player:FindFirstChild("customSpeed") then
                    Update11:Set("Custom Speed: " .. FormatDisplay(player.customSpeed.Value))
                else
                    Update11:Set("Custom Speed: N/A")
                end
                if player:FindFirstChild("evilKarma") then
                    Update12:Set("Evil Karma: " .. FormatDisplay(player.evilKarma.Value))
                else
                    Update12:Set("Evil Karma: N/A")
                end
                if player:FindFirstChild("goodKarma") then
                    Update13:Set("Good Karma: " .. FormatDisplay(player.goodKarma.Value))
                else
                    Update13:Set("Good Karma: N/A")
                end

                local leaderstats = player:FindFirstChild("leaderstats")
                if leaderstats then
                    if leaderstats:FindFirstChild("Strength") then
                        Update:Set("Strength: " .. FormatDisplay(leaderstats.Strength.Value))
                    else
                        Update:Set("Strength: N/A")
                    end
                    if leaderstats:FindFirstChild("Rebirths") then
                        Update2:Set("Rebirth: " .. FormatDisplay(leaderstats.Rebirths.Value))
                    else
                        Update2:Set("Rebirth: N/A")
                    end
                    if leaderstats:FindFirstChild("Kills") then
                        Update5:Set("Kills: " .. FormatDisplay(leaderstats.Kills.Value))
                    else
                        Update5:Set("Kills: N/A")
                    end
                else
                    Update:Set("Strength: N/A")
                    Update2:Set("Rebirth: N/A")
                    Update5:Set("Kills: N/A")
                end

                if player:FindFirstChild("currentMap") then
                    Update9:Set("Current Map: " .. tostring(player.currentMap.Value))
                else
                    Update9:Set("Current Map: N/A")
                end
            else
                Update:Set("Strength:")
                Update1:Set("Gems:")
                Update2:Set("Rebirth:")
                Update3:Set("Agility:")
                Update4:Set("Durability:")
                Update5:Set("Kills:")
                Update6:Set("Muscle King Time:")
                Update9:Set("Current Map:")
                Update10:Set("Custom Size:")
                Update11:Set("Custom Speed:")
                Update12:Set("Evil Karma:")
                Update13:Set("Good Karma:")
            end
        else
            Update:Set("Strength:")
            Update1:Set("Gems:")
            Update2:Set("Rebirth:")
            Update3:Set("Agility:")
            Update4:Set("Durability:")
            Update5:Set("Kills:")
            Update6:Set("Muscle King Time:")
            Update9:Set("Current Map:")
            Update10:Set("Custom Size:")
            Update11:Set("Custom Speed:")
            Update12:Set("Evil Karma:")
            Update13:Set("Good Karma:")
        end
    end
end)

Stats:Divider()

local advancedStatsLabel = Stats:Label("Advanced Stats:")

local enemyHealthLabel = Stats:Label("Enemy Health: N/A")
local playerDamageLabel = Stats:Label("Your Damage: N/A")
local hitsToKillLabel = Stats:Label("Hits to Kill: N/A")

local function calculateEnemyHealth(targetPlayer)
    if not targetPlayer then return 0 end
    local baseDura = 0
    local durabilityStat =
        targetPlayer:FindFirstChild("Durability") or
        (targetPlayer:FindFirstChild("leaderstats") and targetPlayer.leaderstats:FindFirstChild("Durability"))
    if durabilityStat then
        baseDura = durabilityStat.Value
    end
    local totalMultiplier = 1
    local ultFolder = targetPlayer:FindFirstChild("ultimatesFolder")
    if ultFolder then
        local infernalHealth = ultFolder:FindFirstChild("Infernal Health")
        if infernalHealth then
            totalMultiplier = totalMultiplier + 0.15 * (infernalHealth.Value or 0)
        end
    end
    local backpack = targetPlayer:FindFirstChild("Backpack")
    if backpack then
        local equippedPets = backpack:FindFirstChild("EquippedPets") or backpack:FindFirstChild("equippedPets")
        if equippedPets then
            for _, pet in ipairs(equippedPets:GetChildren()) do
                if string.lower(tostring(pet)):match("mighty") and string.lower(tostring(pet)):match("monster") then
                    totalMultiplier = totalMultiplier + 0.5
                    break
                end
            end
        end
    end
    return baseDura * totalMultiplier
end

local function calculateLocalPlayerDamage()
    local LocalPlayer = game.Players.LocalPlayer
    if not LocalPlayer then return 0 end
    local strengthStat = nil
    local leaderstats = LocalPlayer:FindFirstChild("leaderstats")
    if leaderstats then
        strengthStat = leaderstats:FindFirstChild("Strength")
    end
    if not strengthStat then return 0 end
    local baseDamage = strengthStat.Value * 0.0667
    local totalMultiplier = 1
    local ultFolder = LocalPlayer:FindFirstChild("ultimatesFolder")
    if ultFolder then
        local demonDamage = ultFolder:FindFirstChild("Demon Damage")
        if demonDamage then
            totalMultiplier = totalMultiplier + math.min(0.1 * (demonDamage.Value or 0), 0.5)
        end
    end
    local backpack = LocalPlayer:FindFirstChild("Backpack")
    if backpack then
        local equippedPets = backpack:FindFirstChild("EquippedPets") or backpack:FindFirstChild("equippedPets")
        if equippedPets then
            for _, pet in ipairs(equippedPets:GetChildren()) do
                if string.lower(tostring(pet)):match("wild") and string.lower(tostring(pet)):match("wizard") then
                    totalMultiplier = totalMultiplier + 0.5
                    break
                end
            end
        end
    end
    return baseDamage * totalMultiplier
end

local function calculateHitsToKill(health, damage)
    if damage <= 0 then return "∞" end
    local hits = math.ceil(health / damage)
    if hits > 50 then
        return "∞"
    elseif hits < 1 then
        return 1
    else
        return hits
    end
end

local function updateAdvancedStats(targetPlayer)
    if not targetPlayer then
        enemyHealthLabel:Set("Enemy Health:")
        playerDamageLabel:Set("Your Damage:")
        hitsToKillLabel:Set("Hits to Kill:")
        return
    end
    local enemyHealth = calculateEnemyHealth(targetPlayer)
    local playerDamage = calculateLocalPlayerDamage()
    local hitsToKill = calculateHitsToKill(enemyHealth, playerDamage)
    enemyHealthLabel:Set("Enemy Health: " .. FormatDisplay(enemyHealth))
    playerDamageLabel:Set("Your Damage: " .. FormatDisplay(playerDamage))
    hitsToKillLabel:Set("Hits to Kill: " .. tostring(hitsToKill))
end

task.spawn(function()
    while task.wait(0.3) do
        local selectedPlayer = game.Players:FindFirstChild(SelectPlayerName)
        updateAdvancedStats(selectedPlayer)
    end
end)

local Killer = Window:Tab({
    Title = "Destroy Anyone",
    Locked = false
})

local playerWhitelist = {}
local targetPlayerNames = {}
local autoGoodKarma = false
local autoBadKarma = false
local autoKill = false
local killTarget = false
local spying = false
local autoEquipPunch = false
local targetDropdownItems = {}
local availableTargets = {}
local selectedTarget = nil
local targetPlayerName = nil
local autoKillBring = false
local friendWhitelistActive = false

local titleLabel1 = Killer:Label("Select Damage or Durability Pet")

local petDropdownRef = nil
local petDropdownItems = {}

local function rebuildPetDropdown()
    local petsFolder = LocalPlayer:FindFirstChild("petsFolder")
    local petNames = {}
    
    if petsFolder then
        local unique = petsFolder:FindFirstChild("Unique")
        if unique then
            local seenPets = {}
            for _, pet in ipairs(unique:GetChildren()) do
                if not seenPets[pet.Name] then
                    seenPets[pet.Name] = true
                    table.insert(petNames, pet.Name)
                end
            end
        end
    end
    
    if #petNames == 0 then
        petNames = {"Wild Wizard", "Mighty Monster"}
    end
    
    if petDropdownRef then
        pcall(function()
            petDropdownRef:Destroy()
        end)
    end
    
    local dropdownOptions = {
        Title = "Select Pet",
        Multi = false,
        Value = "",
        AllowNone = true,
        Values = petNames,
        Callback = function(text)
            local petsFolder = game.Players.LocalPlayer.petsFolder
            for _, folder in pairs(petsFolder:GetChildren()) do
                if folder:IsA("Folder") then
                    for _, pet in pairs(folder:GetChildren()) do
                        game:GetService("ReplicatedStorage").rEvents.equipPetEvent:FireServer("unequipPet", pet)
                    end
                end
            end
            task.wait(0.2)

            local petName = text
            local petsToEquip = {}

            for _, pet in pairs(game.Players.LocalPlayer.petsFolder.Unique:GetChildren()) do
                if pet.Name == petName then
                    table.insert(petsToEquip, pet)
                end
            end

            local maxPets = 8
            local equippedCount = math.min(#petsToEquip, maxPets)

            for i = 1, equippedCount do
                game:GetService("ReplicatedStorage").rEvents.equipPetEvent:FireServer("equipPet", petsToEquip[i])
                task.wait(0.1)
            end
        end
    }
    
    petDropdownRef = Killer:Dropdown(dropdownOptions)
end

rebuildPetDropdown()

Killer:Toggle({
    Title = "Auto Good Karma",
    Value = false,
    Callback = function(bool)
        autoGoodKarma = bool
        task.spawn(function()
            while autoGoodKarma do
                local playerChar = LocalPlayer.Character
                local rightHand = playerChar and playerChar:FindFirstChild("RightHand")
                local leftHand = playerChar and playerChar:FindFirstChild("LeftHand")
                if playerChar and rightHand and leftHand then
                    for _, target in ipairs(Players:GetPlayers()) do
                        if target ~= LocalPlayer then
                            local evilKarma = target:FindFirstChild("evilKarma")
                            local goodKarma = target:FindFirstChild("goodKarma")
                            if evilKarma and goodKarma and evilKarma:IsA("IntValue") and goodKarma:IsA("IntValue") and evilKarma.Value > goodKarma.Value then
                                local rootPart = target.Character and target.Character:FindFirstChild("HumanoidRootPart")
                                if rootPart then
                                    firetouchinterest(rightHand, rootPart, 1)
                                    firetouchinterest(leftHand, rootPart, 1)
                                    firetouchinterest(rightHand, rootPart, 0)
                                    firetouchinterest(leftHand, rootPart, 0)
                                end
                            end
                        end
                    end
                end
                task.wait(0.01)
            end
        end)
    end
})

Killer:Toggle({
    Title = "Auto Bad Karma",
    Value = false,
    Callback = function(bool)
        autoBadKarma = bool
        task.spawn(function()
            while autoBadKarma do
                local playerChar = LocalPlayer.Character
                local rightHand = playerChar and playerChar:FindFirstChild("RightHand")
                local leftHand = playerChar and playerChar:FindFirstChild("LeftHand")
                if playerChar and rightHand and leftHand then
                    for _, target in ipairs(Players:GetPlayers()) do
                        if target ~= LocalPlayer then
                            local evilKarma = target:FindFirstChild("evilKarma")
                            local goodKarma = target:FindFirstChild("goodKarma")
                            if evilKarma and goodKarma and evilKarma:IsA("IntValue") and goodKarma:IsA("IntValue") and goodKarma.Value > evilKarma.Value then
                                local rootPart = target.Character and target.Character:FindFirstChild("HumanoidRootPart")
                                if rootPart then
                                    firetouchinterest(rightHand, rootPart, 1)
                                    firetouchinterest(leftHand, rootPart, 1)
                                    firetouchinterest(rightHand, rootPart, 0)
                                    firetouchinterest(leftHand, rootPart, 0)
                                end
                            end
                        end
                    end
                end
                task.wait(0.01)
            end
        end)
    end
})

Killer:Toggle({
    Title = "Auto Whitelist Friends",
    Value = false,
    Callback = function(state)
        friendWhitelistActive = state

        if state then
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and LocalPlayer:IsFriendsWith(player.UserId) then
                    playerWhitelist[player.Name] = true
                end
            end

            Players.PlayerAdded:Connect(function(player)
                if friendWhitelistActive and player ~= LocalPlayer and LocalPlayer:IsFriendsWith(player.UserId) then
                    playerWhitelist[player.Name] = true
                end
            end)
        else
            for name in pairs(playerWhitelist) do
                local friend = Players:FindFirstChild(name)
                if friend and LocalPlayer:IsFriendsWith(friend.UserId) then
                    playerWhitelist[name] = nil
                end
            end
        end
    end
})

local whitelistDropdownRef = nil
local whitelistDropdownItems = {}

local function rebuildWhitelistDropdown()
    local playerNames = {}
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and not playerWhitelist[player.Name] then
            table.insert(playerNames, getPlayerDropdownText(player))
        end
    end
    
    if whitelistDropdownRef then
        pcall(function()
            whitelistDropdownRef:Destroy()
        end)
    end
    
    local dropdownOptions = {
        Title = "Whitelist Player",
        Multi = false,
        Value = "",
        AllowNone = true,
        Values = playerNames,
        Callback = function(displayName)
            local playerName
            if displayName:find(" | ") then
                playerName = displayName:match(" | (.+)")
            else
                playerName = displayName
            end
            
            local target = Players:FindFirstChild(playerName)
            if target and target ~= LocalPlayer then
                playerWhitelist[target.Name] = true
                pcall(function()
                    whitelistDropdownRef:SetValue("")
                end)
                updateWhitelistLabel()
                rebuildWhitelistDropdown()
            end
        end
    }
    
    whitelistDropdownRef = Killer:Dropdown(dropdownOptions)
end

rebuildWhitelistDropdown()

Players.PlayerAdded:Connect(function(player)
    if player ~= LocalPlayer then
        task.wait(0.5)
        rebuildWhitelistDropdown()
    end
end)

Players.PlayerRemoving:Connect(function(player)
    if player ~= LocalPlayer then
        task.wait(0.5)
        rebuildWhitelistDropdown()
    end
end)

Killer:Toggle({
    Title = "Auto Kill",
    Value = false,
    Callback = function(bool)
        autoKill = bool

        task.spawn(function()
            while autoKill do
                local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                local rightHand = character:FindFirstChild("RightHand")
                local leftHand = character:FindFirstChild("LeftHand")

                local punch = LocalPlayer.Backpack:FindFirstChild("Punch")
                if punch and not character:FindFirstChild("Punch") then
                    punch.Parent = character
                end

                if rightHand and leftHand then
                    for _, target in ipairs(Players:GetPlayers()) do
                        if target ~= LocalPlayer and not playerWhitelist[target.Name] then
                            local targetChar = target.Character
                            local rootPart = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
                            if rootPart then
                                pcall(function()
                                    firetouchinterest(rightHand, rootPart, 1)
                                    firetouchinterest(leftHand, rootPart, 1)
                                    firetouchinterest(rightHand, rootPart, 0)
                                    firetouchinterest(leftHand, rootPart, 0)
                                end)
                            end
                        end
                    end
                end

                task.wait(0.05)
            end
        end)
    end
})

local targetDropdownRef = nil
local targetDropdownItems = {}

local function rebuildTargetDropdown()
    local playerNames = {}
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            table.insert(playerNames, player.DisplayName)
        end
    end
    
    if targetDropdownRef then
        pcall(function()
            targetDropdownRef:Destroy()
        end)
    end
    
    local dropdownOptions = {
        Title = "Select Target",
        Multi = false,
        Value = "",
        AllowNone = true,
        Values = playerNames,
        Callback = function(displayName)
            for _, player in ipairs(Players:GetPlayers()) do
                if player.DisplayName == displayName then
                    if not table.find(targetPlayerNames, player.Name) then
                        table.insert(targetPlayerNames, player.Name)
                    end
                    selectedTarget = player.Name
                    break
                end
            end
        end
    }
    
    targetDropdownRef = Killer:Dropdown(dropdownOptions)
end

rebuildTargetDropdown()

Players.PlayerAdded:Connect(function(player)
    if player ~= LocalPlayer then
        task.wait(0.5)
        rebuildTargetDropdown()
    end
end)

Players.PlayerRemoving:Connect(function(player)
    for i = #targetPlayerNames, 1, -1 do
        if targetPlayerNames[i] == player.Name then
            table.remove(targetPlayerNames, i)
        end
    end
    task.wait(0.5)
    rebuildTargetDropdown()
end)

Killer:Button({
    Title = "Remove Selected Target",
    Callback = function()
        if selectedTarget then
            for i, v in ipairs(targetPlayerNames) do
                if v == selectedTarget then
                    table.remove(targetPlayerNames, i)
                    break
                end
            end
            selectedTarget = nil
        end
    end
})

Killer:Toggle({
    Title = "Kill Target (No Bring)",
    Value = false,
    Callback = function(state)
        killTarget = state

        task.spawn(function()
            while killTarget do
                local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()

                local punch = LocalPlayer.Backpack:FindFirstChild("Punch")
                if punch and not character:FindFirstChild("Punch") then
                    punch.Parent = character
                end

                local rightHand = character:FindFirstChild("RightHand")
                local leftHand = character:FindFirstChild("LeftHand")

                if rightHand and leftHand then
                    for _, name in ipairs(targetPlayerNames) do
                        local target = Players:FindFirstChild(name)
                        if target and target ~= LocalPlayer and target.Character then
                            local rootPart = target.Character:FindFirstChild("HumanoidRootPart")
                            local humanoid = target.Character:FindFirstChild("Humanoid")
                            if rootPart and humanoid and humanoid.Health > 0 then
                                pcall(function()
                                    firetouchinterest(rightHand, rootPart, 1)
                                    firetouchinterest(leftHand, rootPart, 1)
                                    firetouchinterest(rightHand, rootPart, 0)
                                    firetouchinterest(leftHand, rootPart, 0)
                                end)
                            end
                        end
                    end
                end

                task.wait(0.05)
            end
        end)
    end
})

Killer:Toggle({
    Title = "Kill Target (Bring Player)",
    Value = false,
    Callback = function(state)
        autoKillBring = state
        if not state then return end

        task.spawn(function()
            while autoKillBring do
                local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                local myRoot = character:FindFirstChild("HumanoidRootPart")
                
                if myRoot then
                    local punch = LocalPlayer.Backpack:FindFirstChild("Punch")
                    if punch and not character:FindFirstChild("Punch") then
                        punch.Parent = character
                    end

                    for _, name in ipairs(targetPlayerNames) do
                        local target = Players:FindFirstChild(name)
                        if target and target ~= LocalPlayer and target.Character then
                            local humanoid = target.Character:FindFirstChild("Humanoid")
                            local targetRoot = target.Character:FindFirstChild("HumanoidRootPart")
                            
                            if targetRoot and humanoid and humanoid.Health > 0 then
                                pcall(function()
                                    targetRoot.CFrame = myRoot.CFrame * CFrame.new(0, 0, -3)
                                end)

                                pcall(function()
                                    humanoid:TakeDamage(10)
                                end)

                                local rightHand = character:FindFirstChild("RightHand")
                                local leftHand = character:FindFirstChild("LeftHand")
                                if rightHand and leftHand then
                                    pcall(function()
                                        firetouchinterest(rightHand, targetRoot, 1)
                                        firetouchinterest(leftHand, targetRoot, 1)
                                        firetouchinterest(rightHand, targetRoot, 0)
                                        firetouchinterest(leftHand, targetRoot, 0)
                                    end)
                                end
                            end
                        end
                    end
                end

                task.wait(0.2)
            end
        end)
    end
})

local spyTargetDropdownRef = nil

local function rebuildSpyTargetDropdown()
    local playerNames = {}
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer then
            table.insert(playerNames, player.DisplayName)
        end
    end
    
    if spyTargetDropdownRef then
        pcall(function()
            spyTargetDropdownRef:Destroy()
        end)
    end
    
    local dropdownOptions = {
        Title = "Select View Target",
        Multi = false,
        Value = "",
        AllowNone = true,
        Values = playerNames,
        Callback = function(displayName)
            for _, player in ipairs(Players:GetPlayers()) do
                if player.DisplayName == displayName then
                    targetPlayerName = player.Name
                    break
                end
            end
        end
    }
    
    spyTargetDropdownRef = Killer:Dropdown(dropdownOptions)
end

rebuildSpyTargetDropdown()

Players.PlayerAdded:Connect(function(player)
    if player ~= LocalPlayer then
        task.wait(0.5)
        rebuildSpyTargetDropdown()
    end
end)

Players.PlayerRemoving:Connect(function(player)
    task.wait(0.5)
    rebuildSpyTargetDropdown()
end)

Killer:Toggle({
    Title = "View Player",
    Value = false,
    Callback = function(bool)
        spying = bool
        if not spying then
            local cam = workspace.CurrentCamera
            cam.CameraSubject = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") or LocalPlayer
            return
        end
        task.spawn(function()
            while spying do
                local target = Players:FindFirstChild(targetPlayerName)
                if target and target ~= LocalPlayer then
                    local humanoid = target.Character and target.Character:FindFirstChild("Humanoid")
                    if humanoid then
                        workspace.CurrentCamera.CameraSubject = humanoid
                    end
                end
                task.wait(0.1)
            end
        end)
    end
})

Killer:Divider()

